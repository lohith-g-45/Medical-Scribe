fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
httpx==0.25.2

# AI / ML
faster-whisper>=1.2.0,<2.0.0
transformers==4.35.2
torch>=2.1.1,<3.0.0
sentencepiece==0.1.99

# NLP
spacy>=3.6.1,<3.7.0
https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_core_sci_sm-0.5.3.tar.gz

# Voice-based speaker identification (doctor voiceprint enrollment/matching)
resemblyzer>=0.1.4
librosa>=0.10.0
# webrtcvad needs a C compiler; the plain package builds fine on Linux (Render) but not on
# Windows without MSVC Build Tools — use the prebuilt-wheel fork there instead. Both provide
# the same importable `webrtcvad` module name.
webrtcvad>=2.0.10; sys_platform != "win32"
webrtcvad-wheels>=2.0.14; sys_platform == "win32"

# LLM
groq==0.4.2

# Security / Crypto
cryptography==41.0.7
python-jose[cryptography]==3.3.0
PyJWT==2.8.0

# Utilities
numpy==1.26.2
soundfile==0.12.1
pydantic==2.5.2
pydantic-settings==2.1.0
python-dotenv==1.0.0
aiofiles==23.2.1
