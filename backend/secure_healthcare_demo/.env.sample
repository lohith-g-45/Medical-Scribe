# JWT signing key (change in real deployments)
JWT_SECRET_KEY=super-secret-jwt-key-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 32-byte AES key, base64 urlsafe encoded
# Generate with Python:
# import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())
AES_256_KEY_B64=REPLACE_WITH_32_BYTE_BASE64_KEY

# SQLite DB for local demo
DATABASE_URL=sqlite:///./secure_healthcare.db
